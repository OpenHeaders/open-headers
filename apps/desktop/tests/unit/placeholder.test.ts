import { describe, expect, it } from 'vitest';

// Placeholder while the desktop app is being rewritten from scratch.
// Real tests will replace this file as the new src/ tree lands.
describe('desktop placeholder', () => {
  it('compiles', () => {
    expect(true).toBe(true);
  });
});
