{
  "name": "@openheaders/nm-host",
  "version": "2026.8.2",
  "private": true,
  "description": "Open Headers native-messaging bootstrap host — token handoff only, never bulk transport",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "pack:bun": "node scripts/pack-bun.mjs",
    "clean": "rm -rf dist-bun"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
