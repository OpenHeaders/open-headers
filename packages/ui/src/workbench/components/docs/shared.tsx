/**
 * Compatibility re-export — the docs section primitives now live in
 * `@openheaders/ui/shared/docs/shared`. Section files in this directory
 * still import from `./shared` (this file); new code should import
 * from the shared module directly.
 */

export * from '@openheaders/ui/shared/docs/shared';
