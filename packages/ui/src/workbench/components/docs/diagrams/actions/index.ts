/**
 * Actions diagrams — split by diagram.
 *
 *   taxonomy.tsx     — three-category action catalog with engine tags.
 *   rule-anatomy.tsx — BEFORE → RULE → AFTER request-lifecycle pipeline.
 */

export { ActionsRuleAnatomyDiagram } from './rule-anatomy';
export { ActionsTaxonomyDiagram } from './taxonomy';
