/**
 * Pane captions for the two-sided response override views (split and
 * diff). Phrased as the delivery path so the reader can see where each
 * body came from and where Open Headers sat in the middle.
 */

export const RESPONSE_ORIGINAL_LABEL = 'Original · server → browser page';
export const RESPONSE_MODIFIED_LABEL = 'Modified · server → Open Headers → browser page';
