/**
 * Re-export shim. The canonical definitions live in
 * `@openheaders/core/types/editing-scope-view-state`. New code should
 * import from `@openheaders/core/types` directly.
 */

export type {
  DonorRecord,
  EditingScopeViewStateApi,
  EditingScopeViewStateEnvelope,
  SurfaceType,
  UseEditingScopeViewStateOptions,
} from '@openheaders/core/types';
