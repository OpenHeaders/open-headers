/**
 * Workbench namespace — Japanese. Mirrors `catalogs/en/workbench.ts`
 * key for key.
 */

import type { Catalog } from '../../types';

export const workbench = {
  'workbench.settings.search.placeholder': '設定を検索',
  'workbench.settings.search.filter.modified': '変更済み',
} as const satisfies Catalog;
