---
title: "Server"
description: "The self-hosting book: install and service lifecycle, LAN vs TLS, tokens and pairing, users, backup and restore, upgrades, Docker, troubleshooting."
---

This section is being written: the complete self-hosting book —
install and service lifecycle, the LAN vs TLS-proxy decision, tokens
and pairing, users, seats and SSO, backup and restore, upgrades,
Docker and compose, and a troubleshooting matrix.

Until then, the [Server quick start](/quickstart/server) covers the
full happy path — install, first token, LAN reachability, TLS via a
reverse proxy, and Docker — and the daemon's built-in `ohd help`
documents every command.
