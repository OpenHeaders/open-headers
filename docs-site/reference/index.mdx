---
title: "Reference"
description: "Command, configuration, and constants reference — generated from source."
---

This section is being written: the `ohd` and `oh` command reference,
the `daemon.json` configuration reference, settings keys, environment
variables, ports, and exit codes — generated directly from the source
code so it cannot drift from what ships.

Until then, `ohd help` and `oh help` print the complete, current
command tables, and the [Server quick start](/quickstart/server) shows
the configuration precedence chain in action.
